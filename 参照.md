# OpenClaw vs Hermes Agent — 技术参照

> 编写日期：2026-05-13
> 用途：DeepSoulClaw 项目技术选型参考

---

## 一、项目概览

| | OpenClaw | Hermes Agent |
|---|---|---|
| **仓库** | `openclaw/openclaw` | `NousResearch/hermes-agent` |
| **GitHub Stars** | 372K | 148K |
| **Commits** | 49,264 | 8,191 |
| **仓库结构** | 57 个 repo 的生态组织 | 单体仓库 |
| **核心语言** | TypeScript | Python 87.9% + TypeScript 8.9% |
| **包管理** | pnpm monorepo | uv (Python) + npm (TS) |
| **许可证** | MIT | MIT |
| **开发商** | 开源社区 | Nous Research |
| **定位** | "连接一切的手" — 通用自动化编排平台 | "陪你成长的脑" — 自我进化的长期AI伙伴 |

---

## 二、项目目录结构

### 2.1 OpenClaw

```
openclaw/
├── src/                    ← 核心引擎 (agent/gateway/session)
│   ├── agents/             ← Agent 执行引擎
│   ├── auto-reply/         ← 自动回复 + 队列调度
│   └── gateway/            ← 网关控制平面
├── packages/               ← monorepo 子包
├── skills/                 ← SKILL.md 技能定义
├── extensions/             ← 插件系统
├── ui/                     ← Control UI (Web 面板)
├── apps/                   ← 伴侣应用 (macOS/iOS/Android)
├── deploy/                 ← 部署脚本
├── config/                 ← 配置模板
├── docs/                   ← 文档
├── openclaw.mjs            ← 入口文件
├── tsconfig.json           ← TypeScript 配置
└── pnpm-workspace.yaml     ← monorepo 配置
```

核心生态仓库：
- `clawhub` (8.5K★) — 技能注册中心/市场
- `mcporter` (4.4K★) — MCP 协议 TypeScript 桥接
- `Peekaboo` (3.9K★) — macOS 屏幕感知 AI 组件
- `acpx` (2.6K★) — ACP 协议 CLI 客户端
- `gogcli` (7.4K★) — Google Workspace 终端工具

### 2.2 Hermes Agent

```
hermes-agent/
├── agent/                  ← Agent 核心循环
│   ├── __init__.py         ← 核心导出
│   ├── anthropic_adapter.py
│   ├── bedrock_adapter.py
│   ├── context_engine.py   ← 上下文引擎
│   ├── context_compressor.py
│   ├── curator.py          ← 对话策展器
│   ├── memory_manager.py   ← 记忆管理器
│   ├── memory_provider.py  ← 记忆提供者抽象
│   ├── prompt_builder.py   ← 提示构建器
│   ├── skill_commands.py   ← 技能命令
│   ├── skill_preprocessing.py
│   └── tool_guardrails.py  ← 工具安全护栏
├── gateway/                ← 消息网关 (Telegram/Discord/Slack...)
├── skills/                 ← 40+ 内置技能
├── providers/              ← LLM 提供商适配
├── tools/                  ← 工具系统
├── cron/                   ← 定时任务调度
├── environments/           ← RL/Atropos 训练环境
├── docker/                 ← 容器化
├── web/                    ← Web 界面
├── hermes                  ← 主入口脚本
├── cli.py                  ← CLI 入口
├── rl_cli.py               ← RL CLI
├── run_agent.py            ← Agent 运行脚本
├── batch_runner.py         ← 批量轨迹生成
├── trajectory_compressor.py← 轨迹压缩器
└── pyproject.toml          ← Python 项目配置
```

---

## 三、架构设计

### 3.1 OpenClaw：Hub-and-Spoke 轴辐式

```
WhatsApp/Telegram/Slack/Discord/微信/QQ/飞书...
           │
  ┌────────▼────────────────────────┐
  │   GATEWAY (port 18789)          │
  │   控制平面 — 消息路由/分发       │
  │   管理 sessions/channels/events │
  │   WebSocket 配对移动端           │
  └────────┬────────────────────────┘
           │
  ┌────────▼────────────────────────┐
  │   Lane Queue 双层并发调度        │
  │   Session Lane (串行) → Global  │
  │   Lane (maxConcurrent=4)        │
  └────────┬────────────────────────┘
           │
  ┌────────▼────────────────────────┐
  │   Agent 实例                    │
  │   ├─ main session (完整权限)    │
  │   └─ non-main session (沙箱)    │
  └────────┬────────────────────────┘
           │
  ┌────────▼────────────────────────┐
  │   工作空间 (~/.openclaw/workspace)│
  │   ├─ AGENTS.md (注入提示)       │
  │   ├─ SOUL.md   (人格定义)       │
  │   ├─ TOOLS.md  (工具说明)       │
  │   └─ skills/   (技能目录)       │
  └─────────────────────────────────┘
```

### 3.2 Hermes Agent：自我进化的 Agent 循环

```
         ┌──────────────────────────┐
         │   消息网关 (Gateway)       │
         │   Telegram/Discord/Slack/  │
         │   WhatsApp/Signal/CLI     │
         │   (六平台统一会话状态)     │
         └──────────┬───────────────┘
                    │
         ┌──────────▼───────────────┐
         │   Agent 核心循环          │
         │                          │
         │   做（执行任务）           │
         │      ↓                   │
         │   学（自动生成SKILL.md）   │
         │      ↓                   │
         │   改进（GEPA进化引擎）     │
         │      ↓                   │
         │   循环...                 │
         └──────────┬───────────────┘
                    │
    ┌───────────────┼───────────────┐
    │               │               │
    ▼               ▼               ▼
┌───────┐    ┌──────────┐    ┌──────────┐
│ 三层   │    │  技能系统 │    │  工具系统 │
│ 记忆   │    │ 自动生成  │    │  安全护栏 │
│ 系统   │    │ GEPA进化  │    │  结果分类 │
└───────┘    └──────────┘    └──────────┘
```

---

## 四、并发控制 — OpenClaw Lane Queue

> 这是 OpenClaw 最核心的工程实现，Hermes Agent 没有同级别的并发机制。

### 4.1 双层 Lane 架构

```
消息到达
    │
    ▼
Session Lane (session:xxx)
    ├─ 并发上限：固定 1（强制串行）
    ├─ 确保同一会话消息不冲突
    └─ 每个 sessionId 映射唯一 lane
         │
         ▼
    Global Lane (main)
    ├─ 并发上限：agents.defaults.maxConcurrent（默认 4）
    ├─ 限制同时运行的 Agent 总数
    └─ 可配置，全局生效
         │
         ▼
    Worker 池 → Agent 实例
```

其他 Lane 类型：
| Lane | 并发上限 | 用途 |
|------|----------|------|
| `main` | maxConcurrent (默认4) | 主入站回复 |
| `cron` | cron.maxConcurrentRuns | 定时任务 |
| `cron-nested` | 同上 | cron 子任务 |
| `subagent` | 默认 8 | 子智能体 |
| `nested` | 独立 | 嵌套流程 |

### 4.2 六种队列模式

**类型定义** (`src/auto-reply/reply/queue/types.ts`)：

```typescript
type QueueMode =
  | "steer"           // 立即注入当前流式输出
  | "followup"        // 排队等待下一回合
  | "collect"         // 合并积压消息（默认）
  | "steer-backlog"   // 注入 + 保留副本
  | "interrupt"       // 立即中断
  | "queue";          // 普通 FIFO

type QueueSettings = {
  mode: QueueMode;
  debounceMs?: number;   // 防抖延迟，默认 1000ms
  cap?: number;          // 队列容量上限，默认 20
  dropPolicy?: "old" | "new" | "summarize";  // 默认 summarize
};
```

### 4.3 各模式行为

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| `steer` | 立即注入当前 Agent 流式输出，不中断 | "等等，用 Java 不是 Python" |
| `followup` | 等当前回合结束后排队执行 | 普通新消息 |
| `collect` (默认) | 合并所有积压消息为一条 Prompt | 用户连发多条消息 |
| `steer-backlog` | 注入当前回合 + 保留副本后续处理 | 高优即时指令 |
| `interrupt` | 立即中断当前 Agent 运行 | 紧急"停止" |
| `queue` | 标准 FIFO 排队 | 标准排队场景 |

### 4.4 Collect 模式合并原理

```typescript
// src/auto-reply/reply/queue/drain.ts
if (queue.mode === "collect") {
  const items = queue.items.splice(0, queue.items.length);
  const prompt = buildCollectPrompt({
    title: "[Queued messages while agent was busy]",
    items,
    renderItem: (item, idx) =>
      `---\nQueued #${idx + 1}\n${item.prompt}`.trim(),
  });
  await runFollowup({ prompt, run, enqueuedAt: Date.now() });
}
```

### 4.5 Steer 模式的注入机制

```typescript
// src/agents/pi-embedded-runner/run/lanes.ts
if (shouldSteer && isStreaming) {
  const handle = ACTIVE_EMBEDDED_RUNS.get(sessionId);
  void handle.queueMessage(text);  // 立即插入到流中
}

// 底层使用 pi-agent 框架的 steer 插入
const queueHandle: EmbeddedPiQueueHandle = {
  queueMessage: async (text: string) => {
    await activeSession.steer(text);
  }
};
```

### 4.6 防抖与容量控制

- 默认 1s 防抖窗口（`debounceMs = 1000`）
- 队列容量上限 20 条（`cap = 20`）
- 满时策略：
  - `old` — 丢弃最旧
  - `new` — 丢弃最新
  - `summarize` — 压缩为摘要（**默认**）

### 4.7 核心源码文件

| 文件 | 功能 |
|------|------|
| `src/agents/pi-embedded-runner/run/lanes.ts` | Session/Global Lane 定义与并发控制 |
| `src/auto-reply/reply/queue/types.ts` | 队列类型定义 |
| `src/auto-reply/reply/queue/drain.ts` | 队列排空逻辑 |
| `src/auto-reply/reply/queue/manager.ts` | 队列生命周期管理 |
| `src/auto-reply/reply/queue/scheduler.ts` | 优先级调度与资源分配 |

---

## 五、Gateway 网关

### 5.1 OpenClaw：中央总线式

```
┌──────────────────────────────────────┐
│   GATEWAY — 单一控制平面              │
│   端口: 18789                         │
│   常驻 Node.js 守护进程               │
│                                      │
│   职责:                              │
│   - 管理所有 sessions                 │
│   - 路由 20+ channels 消息            │
│   - WebSocket 配对 iOS/Android       │
│   - 模型路由与切换                    │
│   - 多 Agent 隔离 (工作空间+会话键)   │
└──────────────────────────────────────┘
```

启动命令：
```bash
openclaw gateway --port 18789 --verbose
```

沙箱模型：
| 会话类型 | 权限 | 后端 |
|----------|------|------|
| `main` | 完整主机访问 | 本地 |
| `non-main` | 受限 (bash/process/read/write/edit) | Docker/SSH/OpenShell |

DM 安全：默认 `dmPolicy="pairing"`，未知发送者需配对码。

### 5.2 Hermes Agent：多协议统一网关

```
┌───────────────────────────────────────────┐
│   统一消息网关                              │
│   ├─ Telegram                              │
│   ├─ Discord                               │
│   ├─ Slack                                 │
│   ├─ WhatsApp                              │
│   ├─ Signal                                │
│   ├─ CLI                                    │
│   └─ 语音消息转录                           │
│                                            │
│   特性:                                    │
│   - 六平台共享同一会话状态                  │
│   - 跨平台无缝切换 (Telegram→CLI 继续对话)  │
│   - 200+ LLM 模型适配                      │
│   - 七种执行后端                            │
└───────────────────────────────────────────┘
```

LLM 适配器（`agent/` 目录下）：
- `anthropic_adapter.py` — Claude 系列
- `bedrock_adapter.py` — AWS Bedrock
- `codex_responses_adapter.py` — OpenAI Codex
- `gemini_native_adapter.py` — Gemini 原生
- `gemini_cloudcode_adapter.py` — Gemini Cloud Code
- `copilot_acp_client.py` — GitHub Copilot ACP
- `moonshot_schema.py` — Moonshot/Kimi
- `auxiliary_client.py` — 通用回退适配

七种执行后端：
1. 本地终端
2. Docker 容器（只读根文件系统、权限降级、PID 限制）
3. SSH 远程
4. Singularity（HPC）
5. Modal（Serverless，空闲休眠/按需唤醒）
6. Daytona（Serverless）
7. Vercel Sandbox

---

## 六、记忆系统

### 6.1 OpenClaw：文件化显式记忆

```
~/.openclaw/workspace/
├── AGENTS.md     ← 注入提示 (agent行为说明)
├── SOUL.md       ← 人格定义 (性格/边界/风格)
├── TOOLS.md      ← 工具说明
├── IDENTITY.md   ← Agent身份
├── USER.md       ← 用户画像
└── skills/
    └── <skill>/SKILL.md
```

**工作原理：**
- 会话开始时，将文件内容冻结注入 system prompt
- 会话中修改立即写盘，但当前 prompt 不更新
- 历史会话通过 `sessions_history` 工具按需检索
- **需要人工编写和维护**

Memory 相关工具：`sessions_list`, `sessions_history`, `sessions_send`

### 6.2 Hermes Agent：三层内建 + 八选一外部插件

```
═══════════════════════════════════════════════
  Always Active (内建，永远在线)
═══════════════════════════════════════════════

Layer 1: 冻结系统提示记忆
  ├─ MEMORY.md (2,200 chars 上限)
  │   Agent笔记本：环境信息、项目上下文、经验教训
  ├─ USER.md (1,375 chars 上限)
  │   用户画像：偏好、沟通风格、常用工具
  └─ 冻结快照机制：
      会话开始注入 → 会话中修改立即写盘 → prompt不更新
      (保护 LLM prefix cache)
      操作：add / replace / remove (无 read，已在 prompt 中)

Layer 2: 程序性技能记忆
  ├─ ~/.hermes/skills/*.skill
  ├─ 任务完成 → 自动分析 → 抽象为可复用模式 → 保存
  ├─ 遵循 agentskills.io 开放标准
  └─ 语义匹配召回 → 持续迭代优化

Layer 3: SQLite FTS5 全文搜索 + LLM 摘要
  ├─ 表结构: conversation_fts(content, speaker, timestamp, session_id)
  ├─ FTS5 做精确召回 (人名/项目名/命令不丢)
  ├─ LLM (Gemini Flash) 做语义理解和摘要
  └─ 零运维，$5 VPS 即可

═══════════════════════════════════════════════
  Optional (外部插件，八选一)
═══════════════════════════════════════════════

- Honcho    — 辩证用户建模 (Multi-Peer, 最深集成)
- Mem0      — 服务端事实提取 + 语义搜索
- OpenViking— 文件系统式知识层级
- Hindsight — 知识图谱 + 实体消歧
- Holographic— 本地 SQLite + HRR 代数查询
- +3 个待完善
```

### 6.3 FTS5 为什么不用向量数据库？

| 维度 | FTS5 | 向量数据库 |
|------|------|-----------|
| 运维成本 | 零（SQLite 内建） | 需独立服务 |
| 精确匹配 | 出色（人名/项目名不丢） | 弱 |
| 语义搜索 | 弱 | 原生支持 |
| 部署要求 | $5 VPS | 通常更多资源 |
| 费用 | 免费 | 可能产生费用 |

**补偿方案：** LLM 摘要层补偿 FTS5 的语义短板，两者结合效果优于纯向量检索。

### 6.4 冻结快照的设计深意

```
会话 N                     会话 N+1
读取 MEMORY.md (v1)    →   读取 MEMORY.md (v2)
system prompt 冻结 v1   →   system prompt 冻结 v2
                             (新内容立即生效)
Agent 执行 replace
→ 磁盘写为 v2
→ 当前 prompt 仍为 v1
```

**目的：保护 LLM prefix cache。** 如果 system prompt 频繁变化，cache 不断失效，推理成本和延迟显著增加。冻结快照确保单会话内 prompt 不变，最大化缓存命中率。

**代价：** 记忆更新延迟一个会话周期。

### 6.5 Honcho 辩证用户建模

由 Plastic Labs 开发，核心创新：**不仅记住你说了什么，还推理你的思考方式。**

| 推理轮次 | 内容 |
|----------|------|
| Pass 0 | 冷启动（通用事实）或暖启动（会话上下文） |
| Pass 1 | 自审计 — 识别初始评估空白，综合近期证据 |
| Pass 2 | 调和 — 检查前几轮推理矛盾，产出最终综合 |

**Multi-Peer 架构：** 同一用户可有不同 AI Peer（如编码Peer、写作Peer），每个 Peer 独立构建用户表征，互不污染。

三个调节旋钮：
- `contextCadence` — 基础层刷新频率
- `dialecticCadence` — 辩证调用频率
- `dialecticDepth` — 推理深度 (1-3)

### 6.6 容量管理

- MEMORY.md: 2,200 chars，超过 80% 时主动合并压缩
- USER.md: 1,375 chars
- 逼近上限时返回错误，Agent 必须先清理再添加
- 内置自动去重和注入安全扫描
- 条目用 `§` 分隔

---

## 七、技能系统

### 7.1 OpenClaw：人工编写 + 社区注册中心

```
ClawHub (clawhub.ai, 8.5K★)
    │  社区上传
    ▼
SKILL.md 文件
    │  安装到
    ▼
~/.openclaw/workspace/skills/<skill>/SKILL.md
```

三种技能类型：
- **bundled** — 捆绑内置
- **managed** — 托管安装
- **workspace** — 工作空间自定义

### 7.2 Hermes Agent：自动生成 + 自我进化

```
复杂任务完成
    │
    ▼
分析执行追踪 → 识别成功模式
    │
    ▼
生成 SKILL.md (遵循 agentskills.io 标准)
    │
    ▼
语义匹配召回 → 下次类似任务自动加载
    │
    ▼
GEPA 进化引擎 → 持续迭代优化
    │
    ▼
PR 审批 → 部署更新
```

技能存储：`~/.hermes/skills/`，40+ 内置技能，无限自动生成潜能。

### 7.3 GEPA 进化引擎 (ICLR 2026 Oral)

**全称：** Genetic-Pareto Prompt Evolution（遗传-帕累托提示进化）

**核心流程：**
```
初始化 (加载当前版本为基线)
    ↓
变异 (分析执行追踪 → 找根本原因 → 生成针对性文本变体)
    ↓
评估 (batch_runner 并行运行所有变体)
    ↓
选择 (帕累托最优 — 准确率 × 成本 × 延迟)
    ↓
迭代 (保留最优个体继续进化)
```

**帕累托多目标优化：**

| 维度 | 方向 | 说明 |
|------|------|------|
| 准确率 | 最大化 | 任务完成的质量 |
| 成本 | 最小化 | API token 消耗 |
| 延迟 | 最小化 | 响应时间 |

**安全护栏：**
- 测试套件 100% 通过
- 技能文件 ≤ 15KB，工具描述 ≤ 500 chars
- Schema 冻结（参数名/类型/必填不可变，仅文本可进化）
- 语义保持、事实准确性
- 必须通过 PR 人工审查

**关键指标：**
- 数据效率：仅需 3 个示例
- 每次优化成本：$2-10
- 运行方式：纯 API 调用，无需 GPU
- 改进对象：提示文本/指令/示例（**非模型权重**）

**DSPy 集成：**

| 优化目标 | DSPy 模块 |
|----------|-----------|
| 技能文本 (SKILL.md) | `dspy.Signature` |
| Agent 工作流 | `dspy.ReAct` |
| 工具选择 | `dspy.Predict` |

```
主引擎: dspy.GEPA (反射性进化)
备用引擎: dspy.MIPROv2 (贝叶斯优化)
代码优化: Darwinian Evolver (外部 CLI)
```

**与传统方案对比：**

| 维度 | GEPA | 传统 RL | 手动调优 |
|------|------|---------|----------|
| 需 GPU | ❌ | ✅ | ❌ |
| 每次成本 | $2-10 | $100-1000+ | 人工高 |
| 数据需求 | 3 示例 | 大量 | 靠经验 |
| 可解释性 | 高 (文本改进可见) | 低 (权重黑盒) | 高 |
| 安全性 | 高 (PR+护栏) | 中 | 高 |

---

## 八、OpenClaw 会话模型

### 8.1 会话生命周期

```
创建会话 (sessionKey)
    │
    ▼
注入冻结 Prompt (AGENTS.md + SOUL.md + TOOLS.md + MEMORY.md)
    │
    ▼
Agent 执行循环
    │  ├─ 用户消息 → Agent 处理 → 工具调用 → 回复
    │  ├─ /compact → 压缩上下文(摘要历史)
    │  ├─ /reset   → 重置会话状态
    │  └─ /new     → 创建新会话
    │
    ▼
会话结束 → 持久化历史
```

### 8.2 聊天命令

| 命令 | 作用 |
|------|------|
| `/status` | 查看当前会话状态 |
| `/new` | 创建全新会话 |
| `/reset` | 重置当前会话 |
| `/compact` | 压缩对话上下文（摘要长历史） |
| `/think <level>` | 调整推理深度 |
| `/verbose on\|off` | 详细日志开关 |
| `/trace on\|off` | 执行追踪开关 |
| `/usage off\|tokens\|full` | Token 用量显示 |
| `/restart` | 重启 Agent 守护进程 |
| `/activation mention\|always` | 激活方式（@提及/总是） |

### 8.3 会话工具 API

```typescript
// Agent 可用的会话管理工具
sessions_list     // 列出所有会话
sessions_history  // 检索历史对话
sessions_send     // 向其他会话发送消息
sessions_spawn    // 创建子会话（sub-agent）
```

### 8.4 Agent 配置入口

```json
// ~/.openclaw/openclaw.json
{
  "agent": {
    "model": "<provider>/<model-id>"
  },
  "agents": {
    "defaults": {
      "workspace": "~/.openclaw/workspace",
      "maxConcurrent": 4,
      "sandbox": {
        "mode": "non-main"
      }
    }
  }
}
```

---

## 九、Prompt 构建机制

### 9.1 OpenClaw：多文件注入

```
System Prompt 组装流程：

1. 读取 ~/.openclaw/workspace/AGENTS.md
2. 读取 ~/.openclaw/workspace/SOUL.md
3. 读取 ~/.openclaw/workspace/TOOLS.md
4. 读取 ~/.openclaw/workspace/IDENTITY.md
5. 读取 ~/.openclaw/workspace/USER.md
6. 注入 matched skills (SKILL.md)
7. 注入当前会话上下文
8. 注入工具声明 (browser/canvas/nodes/cron...)
       │
       ▼
   最终 System Prompt → 发送给 LLM
```

**文件头约定：**
```markdown
---
summary: "一句话描述"
read_when:
  - 触发条件 (如"启动时""特定任务时")
---
```

**注入安全：** 文件内容经过 prompt injection 扫描，过滤恶意指令。

### 9.2 Hermes Agent：分层组装

```
System Prompt 组装流程：

1. 冻结快照层
   ├─ MEMORY.md (2,200 chars, Agent 笔记本)
   └─ USER.md   (1,375 chars, 用户画像)

2. 动态上下文字段
   ├─ 当前会话历史
   ├─ FTS5 检索结果 (LLM 摘要化)
   ├─ Honcho 辩证推理结果 (contextCadence 控制频率)
   └─ 匹配的 Skill 文件

3. 安全护栏注入
   ├─ 工具护栏规则 (tool_guardrails.py)
   └─ Shell 钩子约束 (shell_hooks.py)
       │
       ▼
   最终 System Prompt → 发送给 LLM
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `agent/prompt_builder.py` | 组装最终 system prompt |
| `agent/prompt_caching.py` | 管理 prompt prefix cache |
| `agent/context_engine.py` | 收集和组装上下文 |
| `agent/context_compressor.py` | 超出窗口时压缩 |
| `agent/think_scrubber.py` | 清理模型内部思维链 |

### 9.3 上下文压缩

**OpenClaw：** `/compact` 命令触发，LLM 摘要长历史

**Hermes：**
- `context_compressor.py` — 当上下文超出模型窗口时自动触发
- `manual_compression_feedback.py` — 允许 Agent 主动请求压缩
- `trajectory_compressor.py` — 压缩执行轨迹用于训练数据生成
- 压缩策略：保留关键决策点 + 摘要中间步骤

---

## 十、Hermes Agent 工具系统

### 10.1 工具架构

```
工具系统
├── toolsets.py             ← 工具集定义（分类组织）
├── toolset_distributions.py← 工具集分发（按场景分配）
├── tool_guardrails.py      ← 工具安全护栏（调用前校验）
├── tool_result_classification.py ← 工具结果分类
├── model_tools.py          ← 模型相关工具
└── tools/                  ← 具体工具实现
```

### 10.2 工具安全护栏 (tool_guardrails.py)

执行前校验：
- 参数合法性检查
- 文件路径沙箱限制
- 命令审批（高风险操作）
- Shell 钩子拦截（shell_hooks.py）

### 10.3 工具结果分类 (tool_result_classification.py)

| 分类 | 处理 |
|------|------|
| 成功 | 直接返回 |
| 部分成功 | 标记 + 返回 |
| 权限拒绝 | 提示用户授权 |
| 执行失败 | 错误分析 → 重试/降级 |
| 超时 | 中断 + 结果摘要 |

### 10.4 重试与错误处理

| 文件 | 作用 |
|------|------|
| `agent/error_classifier.py` | 分类 API/工具错误类型 |
| `agent/retry_utils.py` | 指数退避重试策略 |
| `agent/rate_limit_tracker.py` | API 速率限制追踪 |
| `agent/nous_rate_guard.py` | Nous 专用速率守卫 |

---

## 十一、Hermes Agent MLOps 训练管道

> 这是 Hermes 独有的能力，OpenClaw 完全没有。

### 11.1 批量轨迹生成

```bash
python batch_runner.py
```

配置文件：`datagen-config-examples/`

功能：
- 并行生成数千条工具调用轨迹
- 自动检查点（中断可续）
- 可配置工作进程数和工具分布
- 输出格式：ShareGPT 格式对话

### 11.2 轨迹压缩

```bash
python trajectory_compressor.py
```

- 将完整执行轨迹压缩到指定 token 预算内
- 保留关键决策点和工具调用
- 去除冗余中间步骤
- 输出可直接用于模型微调

### 11.3 Atropos RL 环境

目录：`environments/`

- 内置 11 种工具调用解析器
- 支持任意模型架构
- 与 Hermes Agent 深度集成
- 可作为独立的 RL 训练实验平台

### 11.4 评估系统

**GEPA 进化中使用 LLM-as-Judge：**

| 评分维度 | 范围 | 含义 |
|----------|------|------|
| 过程遵循度 | 0-1 | 是否按技能规定的步骤执行 |
| 输出正确性 | 0-1 | 输出是否正确、有用、相关 |
| 简洁性 | 0-1 | 是否在 token 预算内完成 |

每个技能有定制的评分标准（Rubric），以**行为描述**而非精确匹配评判。

### 11.5 数据来源

| 来源 | 类型 | 用途 |
|------|------|------|
| 合成生成 | LLM 生成 | GEPA 优化测试用例 |
| SessionDB 挖掘 | 真实使用 | 挖掘用户实际任务 |
| 手工黄金集 | 人工标注 | 高价值测试用例 |
| 自动评估 | Bug注入 | 测试 debug 能力 |

---

## 十二、子智能体并行

### 12.1 OpenClaw

- 通过会话 spawn 创建隔离子会话
- 全局 lane `subagent` 默认并发 8
- 基础进程隔离

### 12.2 Hermes Agent

- **RPC 通信：** 子 agent 之间通过远程过程调用传递结果
- **零上下文成本：** 将多步流水线折叠为单次回合
- **完全隔离：** 每个子 agent 有独立对话上下文 + 独立终端
- 七种执行后端可选

---

## 十三、定时自动化

### 13.1 OpenClaw

- 内置 cron 调度
- 独立 lane (`cron`, `cron-nested`)
- 不阻塞主入站回复

### 13.2 Hermes Agent

- 内置 cron 调度器
- 支持自然语言描述任务
- 结果投递至任意平台
- 场景：日报、备份、审计、晨间简报

---

## 十四、安全模型

### 14.1 OpenClaw

| 会话类型 | 权限 |
|----------|------|
| `main` | 完整主机: bash, process, read, write, edit, sessions_*, browser, canvas, nodes, cron, discord, gateway |
| `non-main` (沙箱) | 受限: bash, process, read, write, edit, sessions_list, sessions_history, sessions_send, sessions_spawn |

沙箱后端：Docker (默认)、SSH、OpenShell

DM 配对安全：
```bash
openclaw pairing approve <channel> <code>
openclaw doctor  # 安全诊断
```

### 14.2 Hermes Agent

- **零遥测：** 无数据外传
- **零 CVE：** 200+ 安全补丁
- 命令审批机制
- DM 配对
- 容器加固（只读根文件系统、权限降级、PID 限制）
- 本地存储（`~/.hermes/`）

---

## 十五、部署方式

### 15.1 OpenClaw

```bash
# 生产安装
npm install -g openclaw@latest
openclaw onboard --install-daemon

# 开发环境
git clone https://github.com/openclaw/openclaw.git
cd openclaw
pnpm install
pnpm openclaw setup
pnpm ui:build
pnpm gateway:watch
```

依赖：Node.js 24（推荐）或 22.16+

发布通道：stable (`latest`)、beta、dev

### 15.2 Hermes Agent

```bash
# Linux / macOS / WSL2 / Termux
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash

# Windows PowerShell (早期 Beta)
irm https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.ps1 | iex

# 开发环境
git clone https://github.com/NousResearch/hermes-agent.git
cd hermes-agent
./setup-hermes.sh
```

两种入口：
```bash
hermes            # 终端 TUI 直接对话
hermes gateway    # 启动消息网关
```

依赖：Python 3.11+, Node.js, ripgrep, ffmpeg
自动安装 uv 包管理器，无需手动配置环境。

---

## 十六、消息平台支持

### 16.1 OpenClaw

WhatsApp, Telegram, Slack, Discord, Google Chat, Signal, iMessage, IRC, MS Teams, Matrix, Feishu(飞书), LINE, Mattermost, Nextcloud Talk, Nostr, Synology Chat, Tlon, Twitch, Zalo, WeChat(微信), QQ, WebChat

**共 20+ 通道**

### 16.2 Hermes Agent

Telegram, Discord, Slack, WhatsApp, Signal, Email, Home Assistant

**6+ 通道，支持语音消息转录**

---

## 十七、Hermes Agent 从 OpenClaw 迁移

```bash
hermes claw migrate
```

自动导入：
- 设置
- 记忆 (MEMORY.md)
- 技能 (SKILL.md)
- API 密钥

保留原有配置不变。

---

## 十八、关键差异总结

| 维度 | OpenClaw | Hermes Agent |
|------|----------|-------------|
| **设计哲学** | 编排一切、连接一切 | 自我成长、越用越聪明 |
| **最强项** | 并发调度、生态广度、IM 覆盖 | 记忆系统、自动技能、GEPA 进化 |
| **最弱项** | 记忆依赖人工 | 并发控制原始、生态不如 OC |
| **记忆层级** | 单层 (文件注入) | 三层 + 八选一外部 |
| **技能来源** | 人工 + 社区 | 自动生成 + 进化 |
| **并发机制** | 双层 Lane Queue + 六模式 | 基本并发 |
| **语言栈** | TypeScript | Python + TypeScript |
| **部署门槛** | 中 (需 Node.js + 配置) | 低 (一条 curl) |
| **数据隐私** | 取决于部署 | 100% 本地，零遥测 |
| **AI 训练** | 无 | 原生 MLOps + RL 管道 |
| **类比** | iOS — 生态成熟 | Arch Linux — 高度可控 |

---

## 十九、2026 最佳实践

**"Hermes 做脑，OpenClaw 做手"**

- **Hermes Agent** — 负责长期记忆、技能学习、用户建模、自我进化
- **OpenClaw** — 负责广泛自动化、多工具编排、IM 全覆盖、并发调度

两者非替代关系，是互补关系。Hermes 有从 OpenClaw 的迁移通道，说明两个项目有亲缘关系（可能共享核心开发团队或代码基础）。

---

## 二十、对 DeepSoulClaw 的启示

1. **并发调度** → 参考 OpenClaw 的 Lane Queue 双层架构 + 六种队列模式
2. **记忆系统** → 参考 Hermes 的三层记忆 + 冻结快照 + FTS5/LLM 混合检索
3. **技能进化** → 参考 Hermes 的自动技能创建 + GEPA 进化引擎
4. **用户建模** → 参考 Honcho 的辩证推理 + Multi-Peer 架构
5. **多端统一** → 参考 Hermes 的跨平台共享会话状态
6. **安全设计** → 参考两者的沙箱隔离 + DM 配对 + 命令审批
7. **部署体验** → 参考 Hermes 的一条 curl 免配置安装
8. **Prompt 构建** → 参考 OpenClaw 的多文件分层注入 + Hermes 的冻结快照/Prefix Cache 保护 + 上下文自动压缩
9. **工具系统** → 参考 Hermes 的安全护栏 (tool_guardrails) + 结果分类 + 错误重试 + 速率限制四层防护
10. **MLOps 训练** → 参考 Hermes 的批量轨迹生成 + 轨迹压缩 + Atropos RL 环境，构建从数据生成到模型微调的完整管道
