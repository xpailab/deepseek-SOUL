# 待解决问题

> 来自实战使用（deepsoul run 开发小说写作工具）的记录

---

## 一、性能问题

### 1. 执行太慢——轮次过多

**现象**：生成一个 900 行的 HTML 文件需要 23 轮 LLM 调用、几分钟。同样的事人类开发者 30 秒写完。

**原因**：Agent 每改一小段代码就调一次 LLM，每轮等 API 响应。20+ 轮串行等待。

**影响**：基本可用，但体感很差。用户等不及。

**方向**：
- "创建/生成"类任务让 LLM 一次性输出完整内容
- 减少非必要的中间轮次
- 或者并行化某些独立操作

### 2. "一次性写入"能力太弱

**现象**：Agent 写代码的方式是"读文件 → 改一小段 → 下一轮 → 再改一小段"，明明一次能写 500 行非要拆成 10 次。

**原因**：Agent 循环设计鼓励了小步验证，但对"生成"类任务过度了。不是 LLM 能力问题——DeepSeek 完全能一次输出长内容。

**方向**：根据任务类型自适应——创建/生成类任务提示 LLM 一次性输出完整内容；调试/修复类任务保持小步快跑。

### 3. 验证过度

**现象**：每次写文件后启动浏览器、执行 JS 验证。成本高、耗时长。

**原因**：编译验证规则（刚加的）对所有文件类型一视同仁。HTML 文件不需要打开浏览器确认。

**方向**：验证操作分级——轻量（文件存在、非空）→ 中等（语法检查）→ 重量（浏览器打开、运行测试）。根据文件类型选择合适级别。

---

## 二、已修复的问题

### 1. rich 库 GBK 编码崩溃 ✅

**症状**：任务执行成功但输出阶段 crash，UnicodeEncodeError on GBK terminal。

**修复**：`soul/cli.py` 在 rich 导入前 `sys.stdout.reconfigure(encoding="utf-8")`。

**commit**: 40509cd

### 2. 自动技能文件名混乱 ✅

**症状**：`.skill` 文件用完整任务描述做文件名，含路径分隔符 `\`。Windows 上被解释为目录层级，文件跑到错误位置。中文任务触发词全空。

**修复**：`soul/memory/procedural.py` 文件名净化（去特殊字符、全角标点、限 50 字符）；触发词从 14 个扩展到 50+。

**commit**: 17b630a, f3c8ca5

### 3. Web UI Markdown 渲染 + 工具调用不可见 ✅

**症状**：用户输入的下划线路径被破坏；流式累积时 markdown 格式丢失；工具调用只显示名称不显示参数。

**修复**：`soul/gateway/server.py` 删除了有 bug 的下划线转义；流式累积改用 data-raw 属性；工具调用展示参数和完整结果。

**commit**: 7c8dc2a, 0bc9ff4

---

## 三、功能缺口

### 1. chat() 和 chat_stream() 大量重复代码

**现象**：两个方法 80% 逻辑相同，修改循环行为时需同步两处（已多次遇到）。

**方向**：抽取公共的 `_execute_loop()` 方法，chat/chat_stream 只负责输出方式差异。

### 2. 缺少"创建模式"vs"调试模式"自动切换

**现象**：Agent 对所有任务用同样的执行策略。实际上"创建一个新项目"和"修复一个 bug"需要完全不同的执行节奏。

**方向**：首轮侦察后判断任务类型，切换执行策略：
- 创建模式：一次性输出大块内容，减少验证轮次
- 调试模式：小步快跑，每步严格验证

### 3. 测试覆盖率仅 33 个文件中的采样

**现象**：184 个测试看起来不少，但大部分模块只有基础结构测试。`agent.py` 的核心循环没有集成测试（当前测试用 mock LLM 跳过真实循环）。

**方向**：增加端到端集成测试（用 mock LLM + 真实工具调用），覆盖完整的 chat 循环。

---

## 四、工具层问题

*以下来自"2026世界杯深度调研"任务的实战记录*

### 1. Web 搜索工具不可靠

**现象**：Agent 多次报告"搜索没返回相关结果""搜索被严重本地化干扰"。搜索工具返回空结果或无关本地内容。

**影响**：Agent 被迫改用浏览器工具手动访问百度百科，增加了约 10 轮不必要的工具调用。

**方向**：检查 `soul/tools/builtin/web.py` 的搜索引擎配置。当前使用多引擎兜底（Bing→DuckDuckGo→百度→搜狗），但实际效果差。可能需要：
- 修复搜索引擎的查询格式
- 增加直接抓取 Wikipedia / 百度百科 的能力
- 或干脆用 LLM 的训练知识（对时效性不强的主题）

### 2. 部分网站被拦截

**现象**：懂球帝等中文体育网站被拦截（可能是反爬或网络限制）。Agent 需要多次重试不同来源。

**影响**：某些垂直领域（体育、财经）的信息获取困难。

**方向**：在 WebTool 中增加更真实的 UA 头、支持 cookie、或提供代理配置。

### 3. 搜索→失败→重试→换源 循环拖慢执行

**现象**：调研任务 27 轮中，约 10 轮花在"搜索失败 → 换搜索引擎 → 又失败 → 换浏览器"的重试上。实际信息整理和写作只需要约 10 轮。

**影响**：任务总耗时翻倍。

**方向**：工具失败后的重试应该更快收敛——如果 2 次搜索都失败，直接跳过搜索阶段，用 LLM 训练知识替代。不要给 LLM 无限重试的期望。

**修复状态** ✅ 已部分修复 (209ec6e)：浏览器 UA + 并发请求 + Wikipedia 兜底。实测轮数从 27→13（↓52%）。

### 4. 英文搜索质量仍然不佳

**现象**：中美关系调研中，"英文搜索质量不佳"，Bing 虽然返回结果但偏向中文内容（因为 setlang=zh-Hans）。Agent 需要英文资料时无法获取。

**方向**：搜索时根据 query 语言自动选择搜索引擎语言参数。或提供一个 `language` 参数让 LLM 指定搜索语言。

### 5. 深度内容仍需浏览器

**现象**：搜索能返回摘要和链接，但详细内容（外交部声明全文、经济数据表格）仍需用浏览器工具单独抓取。

**方向**：在 web fetch 中增加正文提取能力（readability 算法），让 Agent 能直接从搜索结果 URL 获取全文而不必打开浏览器。
