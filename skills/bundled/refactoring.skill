---
name: refactoring
version: 1.0.0
description: 安全重构 — 提取方法/类、消除重复、改善设计，保持行为不变
type: bundled
triggers: 重构, refactor, 提取, extract, 优化结构, 设计模式, 消除重复, 解耦, 模块化, 技术债
---

# 安全重构

## 描述
在不改变外部行为的前提下改善代码结构。遵循小步前进、持续验证的原则。

## 执行步骤

1. 明确重构目标 — 性能/可读性/可测试性/可扩展性
2. 编写特征测试 — 用测试锁定当前行为（若无已有测试）
3. 识别坏味道 — 长函数/大类/重复代码/过度耦合/数据泥团
4. 选择重构手法：
   - 提取函数 (extract method) — 长函数分解
   - 提取类 (extract class) — 大类拆分
   - 以多态替换条件 — switch/if-else 优化
   - 引入参数对象 — 过长的参数列表
   - 替换算法 — 用更高效的算法
5. 逐步执行 — 每步小改动 → 运行测试 → 提交（绿色提交）
6. 清理遗留代码 — 删除废弃接口、更新调用方
7. 更新文档 — 反映新结构

## 安全守则
- 每次改动后立即运行全套测试
- 不使用 IDE 自动重构（大项目需人工确认影响范围）
- 保持公开 API 向后兼容（先标记 deprecated，再移除）
- 重构和加功能分开提交，不要混在一起

## 使用的工具
bash, file

## 注意事项
- 优先重构高频修改的模块（80/20 法则）
- 不要为了"好看"过度抽象，3 次重复再提取
- 重构前确保有足够的测试覆盖率（至少 70%）
